# Secure-Low-Power-Radio-Communication-Protocol-over-LoRa-
it designs a custom communication protocol from scratch that runs on top of the raw LoRa radio layer, with its own packet structure, its own encryption scheme, its own acknowledgement system, and its own retry logic.
